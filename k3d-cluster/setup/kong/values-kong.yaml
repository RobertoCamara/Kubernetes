env:
  database: "postgres"
  pg_user: postgres
  pg_password: mypostgresql
  pg_database: kong
  pg_host: postgres-external
  pg_port: 5432

ingressController:
  installCRDs: false

proxy:
  type: LoadBalancer
  http:
    enabled: true
    servicePort: 80
  tls:
    enabled: false

admin:
  enabled: true
  type: LoadBalancer
  http:
    enabled: true
    servicePort: 30085

postgresql:
  enabled: false
